import HomeClient from './HomeClient'

export const metadata = {
  title: 'SB: Sales Page Builder',
  description: 'Create compelling sales pages for clients.',
}

export default function Home() {
  return <HomeClient />
}
